<%= form_with(model: cricketer) do |form| %>
  <% if cricketer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cricketer.errors.count, "error") %> prohibited this cricketer from being saved:</h2>

      <ul>
        <% cricketer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.rich_text_area :body%>
  </div>

  <div>
  <%= form.label :image %>
  <%= form.file_field :image%>
  </div>

  <div>
  <%= form.label :pictures %>
  <%= form.file_field :pictures, multiple: true%>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
