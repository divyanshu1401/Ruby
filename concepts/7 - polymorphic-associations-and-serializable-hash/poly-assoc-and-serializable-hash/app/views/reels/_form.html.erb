<%= form_with(model: reel) do |form| %>
  <% if reel.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reel.errors.count, "error") %> prohibited this reel from being saved:</h2>

      <ul>
        <% reel.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :imageUrl, style: "display: block" %>
    <%= form.text_field :imageUrl %>
  </div>

  <div>
    <%= form.label :caption, style: "display: block" %>
    <%= form.text_field :caption %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
